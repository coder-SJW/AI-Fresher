# Skills架构方法论：从Workflow到可进化的Agent系统

> **来源**：宝玉(@dotey)技术分析推文
> **发布时间**：2026年1月11日
> **原始链接**：[你可能不再需要 workflow，大部分场景 skills 足矣——五步框架把 Workflow 变成可进化的 Skill](https://x.com/dotey/status/2010176124450484638)
> **核心观点**：大部分可视化workflow编排场景都可以被Agent + Skills取代

---

## 📋 目录

- [核心论点](#核心论点)
- [讨论背景](#讨论背景)
- [Workflow编排的舒适区与硬伤](#workflow编排的舒适区与硬伤)
- [Agent + Skills的降维打击](#agent--skills的降维打击)
- [Skills架构五步框架](#skills架构五步框架)
- [实战案例：写作工作流](#实战案例写作工作流)
- [三大质疑与回应](#三大质疑与回应)
- [边界在哪里](#边界在哪里)
- [被低估的优势：可进化性](#被低估的优势可进化性)
- [核心洞察总结](#核心洞察总结)

---

## 核心论点

**主要观点**：大部分 workflow 编排场景，都可以被 **Agent + Skills** 取代

**核心理念**：
> *"Skill 应该被看作可组合的模块。用自然语言去编排工作流，而不是用拖拽。"*

---

## 讨论背景

### 起因：一场技术辩论

文章始于宝玉与用户 **pippingg** 的一次技术辩论：

> **pippingg 的观点**：
> *"80 多个节点的 workflow，稳定性和可调整性，不是 subagent 能比拟的。"*

**宝玉的回应**：
> *"这话对，也不对。"*

- ✅ **对的**：传统 workflow 编排的确有核心价值（可预测、可排查、可视化）
- ❌ **不对的**：很多人低估了 AI Agent + Skills 架构的潜力

---

## Workflow编排的舒适区与硬伤

### 舒适区：为什么可视化工具能火？

可视化工作流工具（如Dify）之所以流行，有其合理性：

| 优势 | 说明 |
|------|------|
| **直观易用** | 拖拽节点、连连线，自动化流程就搭好了 |
| **不需要写代码** | 业务人员也能上手 |
| **确定性高** | 节点A执行完一定是节点B，不会跳到节点C |
| **适合审计合规** | 对于需要审计、合规的业务场景，这种确定性很重要 |
| **修改直观** | 改起来比代码更直观 |

**适用场景**：
- 严格审计要求的合规流程（金融、医疗）
- 超高频执行的简单任务
- 非技术用户的可视化需求

### 硬伤：三大致命局限

#### 硬伤一：不够强大 ⭐⭐

**问题**：可视化节点能做的事情有限，复杂逻辑很难表达

**举例**：
- 处理A类文档的workflow，来了B类文档 → 整个流程卡住
- 需要根据内容动态调整处理逻辑 → 节点无法表达
- 复杂的条件判断、循环、异常处理 → 可视化变得极复杂

#### 硬伤二：不够灵活 ⭐⭐

**问题**：一旦流程定死，遇到输入变化就容易出错

**场景**：
```
设计时：处理PDF格式的合同
运行时：来了Word格式、图片格式、扫描件...
结果：Workflow崩溃或需要大量改造
```

#### 硬伤三：难以移植 ⭐⭐

**问题**：平台锁定效应明显

**痛点**：
- 搭了个很厉害的工作流，想给别人用？
- 导出导入一通操作
- 在对方环境里还要调半天
- 不同平台之间几乎不能迁移

---

## Agent + Skills的降维打击

### 核心洞察

> *"我的看法可能比较激进：**几乎所有**能用 workflow 完成的 AI 任务，都可以用 Agent + Skills 实现。"*

### 关键：如何理解 Skill？

**❌ 错误理解**：
- 把 skill 当成单一技能
- 一个 skill 负责翻译，另一个负责总结
- 这样用太浪费了

**✅ 正确理解**：
- **Skill 是可组合的模块**
- 可以把多个 skill 串起来
- 用自然语言描述协作关系
- 换句话说：**用自然语言去编排工作流，而不是用拖拽**

### 为什么是"降维打击"？

| 维度 | 可视化Workflow | Agent + Skills |
|------|---------------|----------------|
| **复杂逻辑表达** | ⭐⭐ 节点能力有限 | ⭐⭐⭐⭐⭐ 自然语言无限制 |
| **灵活性** | ⭐⭐ 流程固定 | ⭐⭐⭐⭐⭐ 自然语言灵活描述 |
| **可移植性** | ⭐⭐ 平台锁定 | ⭐⭐⭐⭐⭐ 文本文件，易分享 |
| **可进化性** | ⭐ 搭完定型 | ⭐⭐⭐⭐⭐ 持续优化、自我迭代 |
| **学习曲线** | ⭐⭐⭐⭐⭐ 零代码 | ⭐⭐⭐ 需要理解自然语言编程 |

---

## Skills架构五步框架

宝玉总结了一个**五步框架**，将传统 Workflow 转化为可进化的 Skill：

### 框架总览

| 步骤 | 名称 | 核心要点 | 关键动作 |
|------|------|----------|----------|
| **1** | **拆分** | 单一职责原则 | 把工作流拆成独立的skill或subagent |
| **2** | **编排** | 自然语言描述 | 用自然语言描述流程，像给同事交代任务 |
| **3** | **存储** | 中间结果持久化 | 所有中间结果保存成本地文件 |
| **4** | **分摊** | 只传路径不传内容 | Subagent之间只传文件路径 |
| **5** | **迭代** | 持续优化进化 | 让AI帮改进prompt，越用越好 |

---

### 第一步：拆分

#### 原则：单一职责

**核心思想**：
> *"把工作流拆成单一职责的 skill 或 subagent。每个模块只做一件事，做好一件事。"*

#### 实战案例：写作工作流拆分

**原始需求**：一篇文章从素材到发布

**拆分结果**：

```
article-analyzer（分析素材）
    ↓ 输出 analysis.md
outliner（生成提纲方案）
    ↓ 输出 outline-a.md, outline-b.md, outline-c.md
writer-agent（写草稿，可并行）
    ↓ 输出 draft-outline-a.md 等
polish（润色定稿）
    ↓ 输出 final.md
```

**拆分的好处**：
- 每个skill职责清晰，易于维护
- 可以独立测试、优化
- 支持并行执行（如3个writer-agent同时跑）

#### 实战案例：配图工作流拆分

```
generate-image（原子技能）
    ↓ 调用图像生成API
article-illustrator（组合技能）
    ↓ 分析文章内容，生成插图
cover-image（组合技能）
    ↓ 生成封面图
```

**要点**：
- 原子技能：调用单一API，如generate-image
- 组合技能：调用其他skill，完成复杂任务

---

### 第二步：编排

#### 核心方法：自然语言描述

**关键**：
> *"在主 skill 里用自然语言描述整个流程。不需要写代码，就像给同事交代任务一样说清楚就行。"*

#### 示例1：outliner技能的编排逻辑

```
"先调用 article-analyzer 分析素材，
分析完成后保存 analysis.md，
然后根据分析结果生成 2-3 个不同风格的提纲方案，
为每个方案并行启动 writer-agent 写草稿。"
```

**说明**：
- ✅ 条件分支："如果...则..."
- ✅ 并行执行："并行启动..."
- ✅ 错误处理："如果失败则..."
- 全部用自然语言表达

#### 示例2：article-illustrator的编排逻辑

```
"读取文章内容，
识别需要配图的位置（概念抽象处、信息密集处、情感转折处），
为每个位置生成图像描述，
调用 generate-image 生成图片，
最后将图片插入文章对应位置。"
```

#### 编排的威力

**一个 skill 可以调用另一个 skill**：
```
写作工作流（主skill）
  ├─ article-analyzer
  ├─ outliner
  │    └─ article-analyzer (复用)
  ├─ writer-agent (可并行启动多个)
  └─ polish

配图工作流
  ├─ article-illustrator
  │    └─ generate-image (原子技能)
  └─ cover-image
       └─ generate-image (复用)
```

**原来在workflow工具里画的每个功能节点，基本都可以对应一个skill。**

---

### 第三步：存储

#### 核心原则：所有中间结果都保存成本地文件

**三个好处**：

| 好处 | 说明 |
|------|------|
| **可追溯** | 出问题了能看到每一步的输出 |
| **可断点续传** | 中途停了，下次从上次的位置继续 |
| **可人工干预** | 不满意某一步的结果，手动改完让Agent继续 |

#### 文件结构示例

**写作流程**：
```
source.md (原始素材)
    ↓
analysis.md (分析结果)
    ↓
outline-a.md (提纲方案A)
    ↓
draft-outline-a.md (草稿)
    ↓
final.md (最终文章)
```

**配图流程**：
```
images/
  ├── article-illustrations/
  │   ├── section-1.png (第一节配图)
  │   └── section-3.png (第三节配图)
  └── cover.png (封面图)
```

**每一步的产出都有迹可循。**

---

### 第四步：分摊

#### 核心规则：只传路径，不传内容

**关键**：
> *"Subagent 之间只传文件路径，不传内容。"*

#### 为什么要这样做？

**问题**：把一大段内容直接塞给subagent
→ 上下文窗口很快就撑满了

**解决**：只传路径
→ subagent自己去读文件
→ 上下文保持干净

#### 示例：writer-agent的参数设计

**❌ 错误做法**：
```javascript
// 传递完整内容
writer_agent.execute({
  source: "这里是几千字的素材...",
  analysis: "这里是分析结果..."
})
```

**✅ 正确做法**：
```javascript
// 只传文件路径
writer_agent.execute({
  sourcePath: "/data/source.md",
  analysisPath: "/data/analysis.md",
  outlinePath: "/data/outline-a.md"
})
// writer-agent自己读取内容，写完后返回输出文件路径
```

#### 额外好处：支持并行

**场景**：3个writer-agent同时跑

```
writer-agent-1: source.md + analysis.md + outline-a.md
writer-agent-2: source.md + analysis.md + outline-b.md
writer-agent-3: source.md + analysis.md + outline-c.md
```

**互不干扰，各自处理一个提纲方案。**

---

### 第五步：迭代

#### 最大优势：可进化性

**关键洞察**：
> *"这是 Agent + Skills 相比传统 workflow **最大的优势**：可以持续进化。"*

#### vs 传统Workflow

| 对比项 | 传统Workflow | Agent + Skills |
|--------|-------------|----------------|
| **改动成本** | 高，需要小心测试 | 低，直接改自然语言描述 |
| **优化方式** | 人工分析、调整 | 让AI帮改进 |
| **长期趋势** | 搭好就开始过时 | 越用越好 |

#### 实践方式

**方式1：手动优化**
```
发现某个skill的提示词不够好？
→ 让Claude Code帮你改
→ 测试效果
→ 迭代改进
```

**方式2：AI辅助优化**
```
用了一段时间，积累了一些问题
→ 直接让AI分析并改进
→ 基于实际使用反馈优化
```

**方式3：自我迭代（pippingg的观点）**
> *"subagent 可以自己迭代 system prompt，配合一些自动化工具，甚至能完成自我迭代进化。在 token 和系统资源充足的情况下，这套系统会变得越来越强。"*

---

## 实战案例：写作工作流

### 完整流程图

```
┌─────────────────────────────────────────────────────────┐
│                    写作工作流（主skill）                    │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
              ┌──────────────────────┐
              │  article-analyzer    │  分析素材
              │                      │  ↓ 输出 analysis.md
              └──────────────────────┘
                          │
                          ▼
              ┌──────────────────────┐
              │     outliner         │  生成提纲方案
              │                      │  ↓ 输出 2-3个outline
              └──────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
  │writer-agent-1│ │writer-agent-2│ │writer-agent-3│
  │(并行)        │ │(并行)        │ │(并行)        │
  └──────────────┘ └──────────────┘ └──────────────┘
          │               │               │
          └───────────────┼───────────────┘
                          ▼
              ┌──────────────────────┐
              │      polish          │  润色定稿
              │                      │  ↓ 输出 final.md
              └──────────────────────┘
```

### 完整配图流程

```
┌─────────────────────────────────────────────────────────┐
│                    配图工作流                            │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
              ┌──────────────────────┐
              │ article-illustrator  │  读取文章
              │                      │  识别配图位置
              └──────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
  │generate-image│ │generate-image│ │generate-image│
  │(原子技能)     │ │(原子技能)     │ │(原子技能)     │
  └──────────────┘ └──────────────┘ └──────────────┘
          │               │               │
          └───────────────┼───────────────┘
                          ▼
              ┌──────────────────────┐
              │    cover-image       │  生成封面图
              │                      │  (2.35:1)
              └──────────────────────┘
```

---

## 三大质疑与回应

### 质疑一：稳定性怎么办？

**质疑声音**：
> *"80 个节点的 workflow 确实经过了反复验证，每个分支都测试过，稳定性有保障。Agent 呢？每次执行可能走不同的路径，结果不可预测。"*

#### 宝玉的回应

**核心观点**：
> *"确定性逻辑不一定要交给 Agent。"*

**解决方案：混合架构**

```
┌─────────────────────────────────────────────────┐
│                  混合架构                         │
├─────────────────────────────────────────────────┤
│                                                 │
│  确定性部分 ───→ 脚本/代码实现                   │
│  (数据转换、格式处理、文件操作)                   │
│                                                 │
│  判断部分 ───→ Agent + Skills实现                │
│  (内容分析、决策生成、创意任务)                   │
│                                                 │
└─────────────────────────────────────────────────┘
```

**举例**：
```
写作流程中的格式化步骤：
- 把中文引号换成全角
- 中英文之间加空格

✅ 这种规则明确的操作 → 写个 format-markdown.ts 脚本
❌ 不要让Agent生成（浪费token且不稳定）

polish技能执行完润色后：
→ 自动调用 format-markdown.ts 处理格式
```

**Anthropic的设计理念**：
> *"Skills 可以包含可执行代码，用于那些传统编程比 token 生成更可靠的任务。"*

---

### 质疑二：成本太高

**质疑声音**：
> *"Agent 执行确实更费 token。每调用一次模型都在烧钱，复杂任务可能要调用几十次。"*

#### 宝玉的回应

**核心观点**：
> *"成本要算总账。"*

**总成本计算**：

| 成本类型 | Workflow | Agent + Skills |
|---------|----------|----------------|
| **开发成本** | 高（节点一个个配） | 低（自然语言描述） |
| **维护成本** | 高（改起来小心翼翼） | 低（灵活调整） |
| **迭代成本** | 高（人工分析优化） | 低（让AI帮改进） |
| **Token成本** | 低 | 高 |
| **总成本** | **???** | **可能更低** |

#### 实战案例：效率提升

**Rakuten（乐天）案例**：
- 用Claude Skills处理财务报表
- 自动处理多个电子表格、捕捉关键异常、按公司流程生成报告
- **原本一天的工作现在一小时完成**
- **8倍效率提升**

**Box案例**：
- 用Skills让用户即时将存储的文件转换为PPT、Excel、Word
- 自动遵循企业风格指南
- **为团队节省了数小时的手工操作**

**结论**：
> *"token 成本在整体效率提升面前根本不算什么。"*

**成本优化策略**：
1. Subagent之间只传文件路径，不传内容
2. 固定逻辑用脚本实现，不要用AI生成
3. 采用"按需加载"设计，只加载当前任务需要的上下文
4. 缓存常见问题的结果

---

### 质疑三：门槛太高

**质疑声音**：
> *"把 workflow 转化成 skill 需要抽象能力。普通用户搭可视化流程可以，让他写 skill 配置文件？太难了。"*

#### 宝玉的回应

**核心观点**：
> *"但这个问题正在被解决。**AI 本身就能帮你创建 skill**。"*

**实践方法**：

使用 `/skill-creator` 等工具：
1. 把需求描述清楚
2. Claude Code帮你生成skill配置
3. 你再根据实际情况调整

**宝玉自己的经验**：
> *"很多 skill 不是我手写的，是让 AI 帮我生成然后再调整。"*

#### 长期优势对比

| 维度 | Workflow | Skill |
|------|----------|-------|
| **创建门槛** | 低（可视化） | 正在降低（AI辅助） |
| **维护难度** | 高（平台锁定） | 低（文本文件） |
| **版本管理** | 困难 | Git管理 |
| **代码审查** | 不支持 | 支持 |
| **跨机器同步** | 困难 | 容易 |

**结论**：
> *"长期来看，skill 比 workflow 更易维护。"*

---

## 边界在哪里

### Agent + Skills 更适合的场景

| 场景特征 | 说明 | 示例 |
|---------|------|------|
| **输入多变** | 需要根据输入灵活调整处理方式 | 处理不同格式的文档、分析各种类型的数据 |
| **跨系统协调** | 需要调用多个API、访问多个数据源 | 企业数据整合、多平台发布 |
| **频繁迭代** | 今天这样做，明天可能要调整 | 内容策略、营销活动 |
| **需要分享复用** | 需要打包发给别人使用 | 开源项目、团队协作工具 |

### Workflow 仍有优势的场景

| 场景特征 | 说明 | 示例 |
|---------|------|------|
| **严格审计要求** | 每一步操作都要可追溯、可审计 | 金融交易、医疗流程 |
| **超高频执行** | 每秒执行几百次的简单操作 | 实时数据处理、高频交易 |
| **非技术用户** | 业务人员需要自己看懂、自己调整 | 小企业的自动化流程 |

### 决策流程图

```
                   需要自动化流程
                         │
                         ▼
              ┌────────────────────┐
              │  是否需要严格审计？  │
              └────────────────────┘
                    │         │
                   是         否
                    │         │
                    ▼         ▼
          ┌──────────────┐  ┌────────────────────┐
          │   Workflow   │  │ 目标用户是否非技术？│
          └──────────────┘  └────────────────────┘
                                │         │
                               是         否
                                │         │
                                ▼         ▼
                      ┌──────────────┐  ┌──────────────┐
                      │  Workflow    │  │ Agent+Skills │
                      └──────────────┘  └──────────────┘
```

---

## 被低估的优势：可进化性

### 核心洞察

> *"Skill 架构有个经常被忽略的好处：**它是活的，可以不断进化**。"*

### vs 传统Workflow

| 对比维度 | 传统Workflow | Agent + Skills |
|---------|-------------|----------------|
| **改动成本** | 高，需人工介入，小心测试 | 低，直接改自然语言 |
| **优化方式** | 人工分析、调整 | 让AI帮改进 |
| **迭代速度** | 慢，周期长 | 快，即时优化 |
| **长期趋势** | 搭好就开始过时 | 越用越好 |

### 进化方式

#### 方式1：手动优化
```
发现问题
  ↓
手动调整prompt
  ↓
测试效果
  ↓
迭代改进
```

#### 方式2：AI辅助优化
```
积累使用数据
  ↓
让AI分析问题
  ↓
AI生成优化建议
  ↓
应用改进
```

#### 方式3：自我迭代（pippingg的激进观点）

**pippingg的观察**：
> *"cc还有一个很强但很少人用的地方，是可以自己迭代subagent的system prompt，配合ralph-loop，已经可以完成自我迭代进化了。在token和系统资源充足的情况下，能进化成非常恐怖的存在。"*

**工作原理**：
```
┌────────────────────────────────────────┐
│          自我迭代循环                   │
├────────────────────────────────────────┤
│                                        │
│  记录每次人工修正                       │
│       ↓                                │
│  用反馈改进prompt                      │
│       ↓                                │
│  将专业知识编码进系统                  │
│       ↓                                │
│  系统越来越强                          │
│                                        │
└────────────────────────────────────────┘
```

**McKinsey的验证**：
> *"在一个法律文档审核流程中，agent 系统会记录每次人工修正，然后用这些反馈来改进自己的 prompt，逐渐将新的专业知识编码进系统。"*

### 最终价值

> *"你投入时间打造的 skill，会随着使用越来越好。而不是像 workflow 那样，搭好就开始慢慢过时。"*

---

## 核心洞察总结

### 十条关键洞察

1. **大部分workflow可以被取代**
   - 几乎所有能用workflow完成的AI任务都可以用Agent + Skills实现

2. **自然语言是终极编排工具**
   - 用自然语言描述协作关系，而不是拖拽节点

3. **Skill是可组合的模块**
   - 一个skill可以调用另一个skill，形成复杂工作流

4. **五步框架**
   - 拆分 → 编排 → 存储 → 分摊 → 迭代

5. **混合架构是最佳实践**
   - 确定性逻辑用代码，判断任务用Agent

6. **只传路径不传内容**
   - Subagent之间只传文件路径，节省token且支持并行

7. **可进化性是最大优势**
   - Skills会越用越好，而不是搭完就过时

8. **成本要算总账**
   - 开发、维护、迭代成本 vs Token成本

9. **AI可以帮你创建skill**
   - 门槛正在降低，AI辅助生成

10. **边界要清晰**
    - 严格审计、超高频、非技术用户 → Workflow
    - 输入多变、跨系统、频繁迭代 → Agent + Skills

### 对创业者的启示

#### 如果你要做Agent开发工具

**机会**：
- Skills编排系统可能取代传统workflow编排
- 自然语言编程是未来趋势
- 可进化性是核心竞争力

**方向**：
1. **Skills开发平台**
   - 让开发者更容易创建、管理、分享skills
   - 提供skill模板市场
   - 支持版本管理、协作

2. **自然语言编排工具**
   - 将自然语言转换为可执行的skill调用
   - 支持条件分支、并行执行、错误处理
   - 可视化调试、监控

3. **Self-improving Agents**
   - 实现自我迭代的agent系统
   - 自动收集反馈、优化prompt
   - 持续学习、进化

#### 如果你要做垂直Agent

**技术选型建议**：
- 不要用可视化workflow限制自己
- 采用Skills架构，保持灵活性
- 建立可进化的系统
- 随着用户增长越来越好用

---

## 参考资料

### 原始来源

- **推文原文**：[你可能不再需要 workflow，大部分场景 skills 足矣——五步框架把 Workflow 变成可进化的 Skill](https://x.com/dotey/status/2010176124450484638)
- **作者**：宝玉(@dotey)
- **发布时间**：2026年1月11日
- **互动数据**：29回复、70转帖、316喜欢、517书签、6.5万观看

### 相关讨论

- **pippingg**：通用agent观点，强调Dify在超复杂任务中的优势
- **Claude Code**：Anthropic的技能架构实现

### 技术参考

- **Claude Code Skills Documentation** - Anthropic
- **Dify开源项目** - Workflow编排平台
- **LangChain** - Agent开发框架

---

## 附录：完整代码示例

### Skills架构示例（伪代码）

```markdown
# outliner skill

## 描述
先调用 article-analyzer 分析素材，
分析完成后保存 analysis.md，
然后根据分析结果生成 2-3 个不同风格的提纲方案，
为每个方案并行启动 writer-agent 写草稿。

## 输入
- sourcePath: 原始素材文件路径

## 输出
- analysis.md: 分析结果
- outline-a.md, outline-b.md: 提纲方案
- draft-outline-a.md, draft-outline-b.md: 草稿

## 子技能调用
1. article-analyzer(sourcePath) -> analysis.md
2. 并行调用:
   - writer-agent(sourcePath, analysis.md, "outline-a方案") -> draft-outline-a.md
   - writer-agent(sourcePath, analysis.md, "outline-b方案") -> draft-outline-b.md
```

---

**文档生成时间**：2026年1月11日
**整理工具**：Claude Code Market Research
**来源**：宝玉(@dotey)技术分析推文
